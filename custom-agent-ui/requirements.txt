# ── Web app ───────────────────────────────────────────────────────────────────
streamlit>=1.40.0
python-dotenv>=1.0.0
requests>=2.32.0
pandas>=2.0.0
openpyxl>=3.1.0

# ── LLM ───────────────────────────────────────────────────────────────────────
openai>=1.50.0
tiktoken>=0.7.0

# ── RAG / Vector store ────────────────────────────────────────────────────────
langchain>=0.3.0
langchain-core>=0.3.0
langchain-community>=0.3.0
langchain-huggingface>=0.1.0
langchain-openai>=0.2.0
langchain-text-splitters>=0.3.0
faiss-cpu>=1.8.0

# ── Embeddings / ML ──────────────────────────────────────────────────────────
# Forces the lightweight CPU wheel (~200 MB) instead of the default GPU wheel (~2 GB)
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.1.0
sentence-transformers>=3.0.0
transformers>=4.40.0
huggingface_hub>=0.24.0
tokenizers>=0.19.0
numpy>=1.26.0
scikit-learn>=1.4.0

# ── Document ingestion (used by pdf_ingest at build/ingest time) ──────────────
PyPDF2>=3.0.0
